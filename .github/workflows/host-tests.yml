name: Host Tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  host-tests:
    name: Build and run host tests
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build gcc

      - name: Configure host all
        run: cmake --preset host-all

      - name: Build host all
        run: cmake --build --preset host-all --verbose

      - name: Run host all
        run: ctest --preset host-all
